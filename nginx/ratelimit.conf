# Rate limit для API чата: 20 запросов/мин на IP (LLM всё равно медленная — это защита
# от абьюза, а не пропускная способность). Ставится в /etc/nginx/conf.d/localllm-ratelimit.conf
limit_req_zone $binary_remote_addr zone=localllm_api:10m rate=20r/m;
